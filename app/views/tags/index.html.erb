<p><%= link_to "Home", root_path %> | <%= link_to "Add a new Happy Place", new_tag_path %></p>


<h1>My Happy Places</h1>

<% @tags.each do |tag| %>
<h3><%= tag.title %></h3>
<p><%= tag.category %></p>
<p><%= tag.comment %></p>
<%= link_to "Edit", edit_tag_path(@tags) %>
<% end %>

